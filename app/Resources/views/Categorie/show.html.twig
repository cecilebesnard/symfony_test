{% extends "layout.html.twig" %}

{% block title %}Show-Categorie{% endblock %}

{% block ariane %}
    <li class="active"><a href="{{ path('categorie') }}">Categories</a></li>
    <li class="active">{{ categorie.title }}</li>
{% endblock %}
{% block content %}

    {% for flash_message in app.session.flashBag.get('success2') %}
        <div class="flash-notice">
            {{ flash_message }}
        </div>
    {% endfor %}

            <h2> {{  categorie.title }} </h2>
            <p> {{ categorie.description }}</p>
            {#<strong> {{ categorie.date_created|date("d/m/Y") }} </strong>#}




{% endblock %}