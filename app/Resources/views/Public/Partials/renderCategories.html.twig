<div class="col-md-3">
    <p class="lead">Shop Name</p>
    <div class="list-group">
        {% for cat in categories %}

            <a href="{{ path("show_publicCategorie",{id:cat.id}) }}" class="list-group-item">{{ cat.title }}</a>



        {% endfor %}

    </div>
</div>